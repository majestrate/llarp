services:
  llarpd-router:
    image: ghcr.io/majestrate/llarp:release
    environment:
      LLARP_DATA_DIR: "/data"
      LLARP_PUBLIC_IP: "public.ip.goes.here"
      LLARP_BIND_UDP_INBOUND: ":1090"
    volumes:
      - data:/data
    ports:
      - "1090:1090/udp"
volumes:
  data:
